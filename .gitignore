node_modules/
frontend/out/
desktop/dist/
.venv/
.env
.env.local

# Next.js build cache and type stubs are outputs, never sources
frontend/.next/
frontend/next-env.d.ts
frontend/tsconfig.tsbuildinfo

# Smoke runs write a report and a throwaway Electron user-data directory
desktop/.smoke/
desktop/.smoke-dist/
frontend/test-results/
frontend/playwright-report/
.vercel
.env*
!.env.example

# Session reports (not ADRs): local working notes, never published. docs/decisions/ itself stays tracked -
# ADR-0001 and future ADRs are decisions this repository publishes.
docs/decisions/reports/
.worktrees/

# pravrudhi kernel state (secret, pools, jobs)
.pravrudhi/

# Loop-local state from the mis-rooted period (2026-09-12): a pravrudhi kernel
# ledger and harness prompts that landed in this product checkout by accident.
# Kept on disk, never committed - research/ evidence and kernel state are local
# by the public/local split, and this is the product repo.
research/
harness/
